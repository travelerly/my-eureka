server:
  port: 8081

spring:
  application:
    name: colin-eureka-consumer

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true